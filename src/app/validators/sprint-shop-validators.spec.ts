import { SprintShopValidators } from './sprint-shop-validators';

describe('SprintShopValidators', () => {
  it('should create an instance', () => {
    expect(new SprintShopValidators()).toBeTruthy();
  });
});
